# Stage 1c receipt — polish + wording sweep

## 0057 and 0059 polish

Six headings, in order, no seventh.

**Receipt — issue 0057, stage 1b re-check**

- **Problem 4 (run_all.sh does not run the lot) — FIXED by wiring, not by weakening the sentence.** `doc/claude/feedback/ngspice_upstream/repro/run_all.sh` section 4 gained two blocks: `save_cmd_lower.cir` (measured `rc=1`, stderr byte-identical to `save_lower.cir`'s — `cmp -s` says yes, `grep -in midnode` over both streams exits 1) and `save_guard_probe.cir` (prints `GUARD-CAPTURED`, and nothing between it and `SHELL-CAT-ABOVE`, so the `shell cat` half is empty as criterion 5 claims). Section 4 now names all ten decks the Status enumerates — `awk '/^hdr "4\./,/^hdr "5\./' run_all.sh | grep -oE '[a-z_]+\.cir'` yields `print_lower`, `save_absent`, `save_cmd_lower`, `save_guard_probe`, `save_lower`, `save_partial_lower` plus the `$d.cir` loop over the four `asym_*`. Script re-run end to end three times, **rc=0** each time. The Status sentence at `:18` stands as written and is now true. Also corrected the script's stale header comment: the baseline binary is used by findings 1, 2, 3, 4, 6 and 8, not "1, 6 and 8" (measured by locating every `$OLD` reference against its enclosing `hdr`).
- **Problem 1 (stale prose about 0056) — FIXED.** Read `doc/codex/issues/0056-save-matches-vector-names-byte-exactly.md` first: its criterion 3 (`:412`) opens `**Deferred to \`doc/codex/issues/0057\`.**` and keeps only the point that the comparison it chooses leaves this report writable; its Resolution (`:478`) opens `No diagnostic.` and points token-naming here. 0057's Impact (was `:277-279`) no longer says 0056 "currently restate[s]" the prescription and "should hand it over" — it now records that 0056 *has* handed it over, quoting both opening fragments. Criterion 3 (was `:416-419`) no longer says "`0056` should defer here in one sentence"; it says 0056 already reads that way. Quotes trimmed to fragments that appear verbatim in 0056, and phrased as "opens" rather than "is the single line", since 0056's criterion 3 is a paragraph.
- **Regression: off-by-one at line 213 — FIXED, checker's count confirmed.** Re-measured on a freshly regenerated `save_lower.raw` (`build-ver_50/src/ngspice -b -n -D casemode=preserve save_lower.cir`, rc=1, 570 bytes, header stamp `Wed Aug 12 19:28:37 UTC 2026` unchanged): `sed -n '1,13p' | grep -c notype` = **5**, `sed -n '14,/^Binary:/p' | grep -c notype` = **7**, total `notype` = **12**, matching `No. Variables: 12`. Text now reads "cut at thirteen lines: eight header lines plus variables 0-4 of the twelve, so the remaining seven variables and the binary payload follow."

Also staged the three files the issue depends on that the previous pass left untracked, matching how it staged the other eight decks: `save_lower.cir`, `print_lower.cir`, `run_all.sh`. The issue file itself is left untracked, as `0056` is. No files under `src/` or `tests/` were touched.

Files:
- `/home/qflow/dev/ngspice_test/doc/codex/issues/0057-a-failed-save-names-no-token.md`
- `/home/qflow/dev/ngspice_test/doc/claude/feedback/ngspice_upstream/repro/run_all.sh`

---

Verified the final state and wrote the receipt.

**Receipt — stage 1c, issue 0059** (`/home/qflow/dev/ngspice_test/doc/claude/batches/2026-08-12-casemode-client-feedback/receipts/stage1c-0059.md`)

**1. FIXED — and the count was four, not three.** Ran all nine decks both ways (`-b -n` vs `-b -n -D casemode=preserve`) with `build-ver_50/src/ngspice`. Six need the flag to produce the quoted output: `save_lower` (570 B constants vs 296 B `Operating Point`), `seq` (341 B + 7-line stderr vs 317 B + empty stderr), `rc0` (570 vs 303), `status_probe` (`1 const`/`0 op1` vs `0 op1`/`0 op2`), `write_named` (rc=1, no file vs rc=0, 324 B), `write_named_capture` (`captured: Warning from checkvalid: …` vs `captured: binary raw file "write_named.raw"`). Two already named it (old lines 39, 222); **four** did not — the checker's three plus `seq.cir`, whose 341-byte raw and byte-identical 7-line stderr exist only under `preserve`. Flag now named at every point of use: the `seq` block, the `status_probe` block (with the `grep` that cuts it), matrix rows 1/2/6 (column is now "deck (flag)"; rows 3 and 7 marked `no -D`), criterion 3 for both `write_named` decks, and the `source rc0.cir` pipe in the `rc` section (measured: with the flag rc=0 over a 570-byte constants raw; without it rc=0 over a legitimate 303-byte `Operating Point` raw). New Status paragraph states which six take `preserve` and which three take no `-D`, matching what `run_all.sh:53,86-112` passes. Re-measured and re-quoted three things touched in passing: `seq.raw`'s `Date:` (`Thu Aug 13 00:42:46  2026`), the `divider.raw` half of the two-tells comparison (now names `repro/divider.cir`; 325 B and a run-time `Date:` under both flags), and `write_named`'s stderr (nine lines — the two quoted are now shown with the `grep` that selects them).

**2. FIXED.** `grep -rn 'plot_cur =' src/` returns 23, the 23rd being `src/frontend/vectors.c:1117` `if (plot_cur == NULL)`; `grep -rn 'plot_cur = ' src/` returns 22. Root Cause now quotes the trailing-space form and says in-line that it is what excludes `vectors.c:1117`. The enumeration and the load-bearing "20 in-process sites" are unchanged.

**3. FIXED by wording, not by a commit.** Nothing under `doc/claude/feedback/` is committed (eight decks `git add`-staged; `save_lower.cir`, `divider.cir`, `run_all.sh` untracked; the six issue files untracked too), so Status now reads "Every deck quoted below **lives under** `doc/claude/feedback/ngspice_upstream/repro/`" and asserts no git state — true whether or not the batch commits the tree later.

Also corrected while re-measuring: Impact's mode-independence sentence said `ngspice-46` "does the same" as the 570-byte case; measured it is 569 (`fold`/`preserve`/`distinguish` all rc=1/570, baseline rc=1/569), now stated.

Six `##` headings unchanged and in order (Status, Summary, Impact, Root Cause, Acceptance Criteria, Resolution), no seventh section; the two pre-existing `###` subsections kept. `src/` and `tests/` untouched. Files changed: `/home/qflow/dev/ngspice_test/doc/codex/issues/0059-write-emits-the-constants-plot-when-no-analysis-ran.md`, `/home/qflow/dev/ngspice_test/doc/claude/batches/2026-08-12-casemode-client-feedback/receipts/stage1c-0059.md` (new).

---

## Batch-wide wording sweep

**Fact check first.** `git log --oneline -1 -- doc/claude/feedback/` is empty — nothing under `doc/claude/feedback/` is in HEAD. `git status --short` shows 27 files under `repro/` staged (`A `) and the rest untracked (`??`). So "committed" was false for every repro artifact; I used "present in the working tree" throughout. The six issue files are themselves untracked, so `git diff` shows nothing — edits are on disk. No commit was made.

**0056** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0056-save-matches-vector-names-byte-exactly.md`
- Status, line 17: "Every deck quoted below is **committed** under `…/repro/` and named where it is used" → "…is **present in the working tree** under…". (The seven decks it lists — `save_lower.cir`, `save_current_lower.cir`, `print_lower.cir`, `plain.cir`, `time_node.cir`, `save_probe.cir`, `save_partial_lower.cir` — are a mix of staged and untracked; three of the seven are staged.)
- All deck paths resolve. No other "committed" claim (line 426 is about a git parent commit, not a deck).

**0057** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0057-a-failed-save-names-no-token.md`
- **No changes.** Re-read from disk: line 13 already says the decks "live in that directory", which is true. Its two remaining "committed" uses (`:296`, `:310`) are about decks under `tests/`, which really are in HEAD (`tests/bsim3soipd/RampVg2.cir`, `tests/regression/case/save-alli-case.cir`, `savecurrents-case.cir` all verified tracked).
- All ten repro paths it names resolve (`asym_*.cir` ×4, `save_lower.cir`, `save_absent.cir`, `save_partial_lower.cir`, `save_cmd_lower.cir`, `save_guard_probe.cir`, `print_lower.cir`, `run_all.sh`).

**0058** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0058-casemode-diagnostics-fire-once-per-file-read.md`
- Status, line 12-14: "Every deck quoted below is **committed** under `…/repro/`…" → "…is **present in the working tree** under…" (paragraph rewrapped, no other wording change).
- Line 50: "on the **committed** `divider.cir` and `count/deck.cir`" → "on the **working-tree** `divider.cir` and `count/deck.cir`". Both are untracked; the surrounding sentence already names the directory.
- Left alone as true: line 195 "The committed deck `tests/xspice/casedist/auto-bridge-node-case-split.cir`" (verified tracked) and line 562 "no committed `.out`" (about `tests/`).
- All paths resolve, including the hidden `repro/count/.spiceinit`, `repro/spiceinit-fold/{.spiceinit,d.cir}` and all ten `repro/reads/*`. `abr_*.cir`, `enr_*.cir`, `inner.cir`, `inner2.cir` are files the decks generate at run time, not repo paths.

**0059** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0059-write-emits-the-constants-plot-when-no-analysis-ran.md`
- **No changes.** Re-read from disk: line 12 already says the decks "live under `…/repro/`". No "committed" claim anywhere in the file.
- All ten paths resolve (`save_lower.cir`, `nocase.cir`, `save_partial_miss.cir`, `seq.cir`, `rc0.cir`, `destroy_curplot.cir`, `status_probe.cir`, `write_named.cir`, `write_named_capture.cir`, `divider.cir`), and `run_all.sh` exists.

**0060** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0060-casemode-variable-reports-the-request-not-the-effect.md`
- Status, line 12: "Every deck quoted below is **committed** under `…/repro/` and is named where it is used" → "…is **present in the working tree** under…".
- Left alone as true: line 251 "in four committed decks" — all four (`tests/regression/case/harness-alive.cir`, `tests/regression/casedist/harness-alive.cir`, `tests/xspice/casedist/harness-alive.cir`, `tests/regression/casedist/vector-pyplot-probe-report.cir`) verified tracked.
- All paths resolve, including `repro/spiceinit/.spiceinit` and the generated `repro/hdr_option.raw`.

**0061** — `/home/qflow/dev/ngspice_test/doc/codex/issues/0061-a-rawfile-option-line-reconfigures-the-next-netlist-read.md`
- Status, line 17-18: "`mix.cir` is **committed** there. The spliced raw headers are built by `repro/hdr_variants.sh`, which is **committed** and takes each one from…" → "`mix.cir` is **present there in the working tree**. The spliced raw headers are built by `repro/hdr_variants.sh`, which is **present there too** and takes each one from…" (line rewrapped).
- Deliberately left: line 20 "the `.raw` products are **not** committed" (true as written, and it is the file's own explanation for the unresolved paths below), plus lines 479 and 556 about `tests/`.

**Deck paths that did not resolve** (all in 0061, none fixed — see below):
`repro/hdr_cmd.raw`, `repro/hdr_cmdset.raw`, `repro/hdr_ngb.raw`, `repro/hdr_numdgt.raw`, `repro/hdr_option_early.raw`, `repro/hdr_optname_mixed.raw`, and the bare-named `hdr_optname_upper.raw`, `hdr_optval_upper.raw`, `hdr_optval_mixed.raw`. I left the paths as written because they are correct: `doc/claude/feedback/ngspice_upstream/repro/hdr_variants.sh` generates every one of those nine names verbatim (`splice hdr_ngb.raw "Option: ngbehavior = hs"` etc.), the directory's `.gitignore` excludes `*.raw`, and 0061 already says these products are regenerated rather than kept. Currently present in that directory are only `ascii_raw.raw`, `hdr_option.raw` and a stale `hdr_newkey.raw` (the last is referenced by no issue and produced by no line of `hdr_variants.sh` — worth a glance from whoever owns `run_all.sh`). Re-running `hdr_variants.sh` restores the other nine.

Every `tests/…` path named across all six files exists on disk and is tracked.

## Re-checks

### One residual misstatement about 0056's text, introduced by the fix to receipt problem 1 and repeated twice. `/home/qflow

verdict: **NEEDS_WORK**

- Receipt problem 4 (RESOLVED, re-measured): the fix landed in the script, not the sentence. /home/qflow/dev/ngspice_test/doc/claude/feedback/ngspice_upstream/repro/run_all.sh section 4 (hdr "4. nothing in the output names the offending token" through hdr "5.") now runs save_lower, print_lower, save_absent, save_partial_lower, save_cmd_lower (lines 138-141), save_guard_probe (line 145) and the four asym_* decks — all ten decks the Status names, so "repro/run_all.sh's finding 4 section runs the lot" (issue line 18) now holds.
- Receipt regression (RESOLVED, re-measured): issue lines 213-214 now read "eight header lines plus variables 0-4 of the twelve, so the remaining seven variables and the binary payload follow." Confirmed by running save_absent.cir (a genuinely absent name, so unaffected by the in-flight 0056 change): constants raw carries No. Variables: 12, 8 header lines, variables 0-4 in the quoted 13, and `sed -n '14,/^Binary:/p' | grep -c notype` = 7.
- Structure and paths OK: exactly six `##` headings in order at lines 3/25/185/313/400/471 (Status, Summary, Impact, Root Cause, Acceptance Criteria, Resolution), no seventh. All ten repro decks exist and are named at point of use; save_partial_lower.raw is 316 bytes listing v(In) and v(all) as claimed. tests/regression/case/save-alli-case.cir:34 and -lower:13 are `save alli`, save-alli-case.cir:13 is the quoted run-level error, savecurrents-case.cir drives inp_savecurrents(), tests/bsim3soipd/RampVg2.cir:15 is the one `.save` in tests/ at 720c8743a (git grep confirms), tests/regression/casedist/vector-unlet-report.cir exists.
- Line references all verified against `git show 720c8743a:` copies (working-tree outitf.c is modified, see below): outitf.c 110/124/168/222/286/300/411/417-422/465-467/479/1338/1358; vectors.c 246/464/478-481/661; dotcards.c 58/74/138(.print branch)/147(.four)/156(.op)/159(.tf); breakp2.c 34/42/126/161; inpsymt.c 143/165/169; subckt.c 1708/1722; evtcheck_nodes.c 910/935; evttermi.c 222/240; com_measure2.c 355/363; identity.baseline:196 = `src/frontend/outitf.c\tstrcmp(n1, n2)` at HEAD; 0001-distinguish.md:76-78 and :93-96 as quoted; 0008 decision 1 is titled "one scan, two reports, and the generic half is mode independent"; FINDINGS finding 4 and finding 7 are the sections the issue cites. The `.save`/name_eq() site is absent from decision 2's table and from decision 3's Class A/Class C tables, as claimed.
- TREE DIVERGENCE, not a defect in the file but it changes what a re-runner will see. A parallel session has an uncommitted 0056 implementation in the working tree: src/frontend/outitf.c (+73/-20, name_eq_query()/name_unwrap() added), a rebuilt /home/qflow/dev/ngspice_test/build-ver_50/src/ngspice (Aug 13 00:50), edits to doc/claude/decisions/0001-distinguish.md and tests/lint/identity.baseline, and three new untracked decks (tests/regression/case/save-name-case.cir:40, save-name-case-lower.cir:15, tests/regression/casedist/save-name-case.cir:30) that each carry a `.save` card. Consequences: (a) the census sentence at issue lines 296-299 is true at the pinned 720c8743a (verified with git grep) but already false in the working tree; (b) the Summary transcript, the asymmetry table's `preserve` rows and criterion 5's evidence no longer reproduce against the rebuilt binary — save_lower.cir now returns rc=0 with v(MidNode) saved, and save_guard_probe.cir prints GUARD-NOT-CAPTURED because its `.save` resolves. The file's "re-run at 720c8743a" pinning covers this, but all of it will need refreshing when 0056 lands.
- Criterion 5's guard claim still holds in substance under the rebuilt binary if the probe deck uses an absent name: I copied save_guard_probe.cir to the scratchpad with v(midnode)->v(nosuchnode) and got GUARD-CAPTURED with nothing between it and SHELL-CAT-ABOVE, i.e. `op >& cap` + fopen/fread/strstr recovers `no data saved` and `shell cat` prints nothing, exactly as the criterion states. Worth re-pointing that criterion's deck at an absent name when 0056 lands, or its named evidence goes quiet.
- Minor, unflagged by the receipt: the asymmetry table has six rows but the transcript pasted below it prints eight, and issue line 120 says "the six rows above are what the runs produce" without noting that the two extra lines (asym_save_nosuch preserve, asym_print_nosuch preserve) are in the transcript. The transcript is a superset, so nothing is false, but the count reads as a mismatch.
- Minor: the Status identifies the binary by build stamp `Wed Aug 12 19:28:37 UTC 2026`, but the rebuilt binary reports the same stamp, so the stamp does not actually pin which binary produced the numbers — only the commit reference does.

### All three receipt items are genuinely fixed, but six of the nine decks no longer reproduce against the binary the file n

verdict: **NEEDS_WORK**

- BLOCKING: six decks (save_lower, seq, rc0, status_probe, write_named, write_named_capture) are documented under `-D casemode=preserve`, which no longer reaches the failure — the working tree's uncommitted outitf.c fix for issue 0056 was linked into build-ver_50/src/ngspice at 00:50. Re-pin those transcripts to `-D casemode=distinguish` (verified to reproduce every stated number), or re-pin the Status section to a binary built from 720c8743a and say so. Affected text: Status lines 21-30, Summary lines 53-106, line 208, lines 245-287, Impact lines 314-316, matrix rows 1/2/6 and lines 462-467, criterion 3 lines 524-542. run_all.sh:54/95/98/105/109/112 need the same change.
- MINOR off-by-one, same class as the one the last pass flagged: Status line 17 puts `save_lower.cir` at `run_all.sh:53`; `:53` is `rm -f save_lower.raw` and the invocation is `:54`. The companion `:86-112` for the other eight is correct (nocase :86, save_partial_miss :92, seq :95, rc0 :98, destroy_curplot :101, write_named :105, write_named_capture :109, status_probe :112).
- CONFIRMED FIXED — receipt item 1 (flag named at point of use): the file now carries a dedicated Status paragraph (lines 21-30), a `deck (flag)` column on every matrix row, the row-6 note at lines 462-464, criterion 3's flag at lines 524-529, criterion 4's at 540-542, and the without-flag reading at 284-287. Every no-`-D` number the receipt measured reproduces exactly: save_lower rc=0 / 296 B / `Plotname: Operating Point`; write_named rc=0 / 324 B; status_probe `after-failed-op status-is 0 curplot-is op1` / `after-good-op status-is 0 curplot-is op2`; write_named_capture `captured: binary raw file "write_named.raw"`; seq rc=0 / empty stderr / 317 B; rc0 rc=0 / 303 B.
- CONFIRMED FIXED — receipt item 2: Status line 12 now reads "lives under", which is accurate; verified nothing under doc/claude/feedback/ is committed (27 paths staged `A `, the rest `??`, including divider.cir).
- CONFIRMED FIXED — the receipt's regression (grep count): lines 366-370 now write `grep -rn 'plot_cur = ' src/` and name the excluded comparison `if (plot_cur == NULL)` at vectors.c:1117. Re-measured: 22 hits with the trailing space, 23 without; vectors.c:1117 is that comparison. Enumeration re-derived and complete — 16 direct + 4 inside plot_setcur() (which spans :1352-1432) + 2 definitions = 22, so the load-bearing '20 in-process sites' figure stands.
- STRUCTURE OK: exactly six `##` headings in order — Status(3), Summary(42), Impact(292), Root Cause(321), Acceptance Criteria(446), Resolution(579) — plus two `###` subsections inside Summary. No seventh section.
- PATHS OK: all nine named decks plus divider.cir resolve under /home/qflow/dev/ngspice_test/doc/claude/feedback/ngspice_upstream/repro/; tests/regression/casedist/vector-unlet-report.cir, doc/claude/decisions/0001-distinguish.md and doc/codex/issues/0032, 0056, 0057 all exist.
- CITATIONS OK: every source line verified against HEAD — outitf.c:461/:479/:1138/:1146/:1149/:1338, vectors.c:166/:174-176/:288/:766/:797/:800-803/:1117/:1352/:1431, plotting.c:7/:13/:14, plot.h:14-16, cpitf.c:41-53, com_let.c:248/:250, postcoms.c:578/:583/:589-594/:611-614/:617/:1090-1092/:1107-1109/:1216, options.c:384-392, rawfile.c:61-64/:115/:117/:618, main.c:1559/:1593, runcoms.c:320/:343/:349, parse.c:290, xpressn.c:1123/:1125, vlnggen:327-333 and :249-261, FINDINGS.md:371-372. The receipt's worry that outitf.c:1338 should be :1337 is unfounded: at HEAD :1337 is `static bool` and :1338 is the `name_eq(char *n1, char *n2)` declarator, so `:1338` is right and consistent with 0056/0057.
- DEFECT ITSELF UNAFFECTED and re-measured verbatim: nocase.cir gives rc=1 + 570-byte `Plotname: constants` under fold, preserve and distinguish, and rc=1 + 569 B on /usr/local/bin/ngspice; save_partial_miss rc=0 / 303 B; all-miss `.save` rc=1 / 570 B; destroy_curplot `curplot-before op1` / `curplot-after const` / rc=0 / 570 B; the rows 3/4/5 pipe gives 570/264/570 with `cmp sc.raw save_lower.raw` IDENTICAL; the bare `write bare.raw` pipe 570 B; `set curplotname` puts `Plotname: Operating Point` over the twelve constants; the Date/Build tell is byte-equal on the bogus raw and differs on divider.raw (325 B under both no-`-D` and preserve, differing only in vector-name case); the fopen/ABSENT probe prints `>   echo ABSENT` then `ABSENT`; the constants raw loads and prints pi/boltz/planck in both binaries.
- Nothing was changed: the issue file, run_all.sh and the decks are untouched, and the one raw I overwrote in the repro directory (save_lower.raw) was restored to its original 570-byte content.
