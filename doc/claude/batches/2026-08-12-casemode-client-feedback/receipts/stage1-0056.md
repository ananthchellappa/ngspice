# Stage 1 receipt — issue 0056

Issue file: `doc/codex/issues/0056-save-matches-vector-names-byte-exactly.md`

## Author receipt

`/home/qflow/dev/ngspice_test/doc/codex/issues/0056-save-matches-vector-names-byte-exactly.md`

**Title:** Issue: `.save` Matches Vector Names Byte-Exactly In Every Case Mode

**Re-measured cleanly:** yes — every assertion in the issue was re-run at `720c8743a` against `build-ver_50/src/ngspice`, with `/usr/local/bin/ngspice` (ngspice-46) as baseline. Verified: the 3x3 node matrix and the 3x3 `i(Vs)`/`i(vs)`/`i(VS)` matrix (18 runs, all rc and resulting vector names quoted); the same matrix via `save` inside `.control` (cell-for-cell identical); `print v(midnode)` = 2.25 under preserve; zero `midnode` hits over stdout+stderr of the failing run; the 570-byte `constants` raw and its header; the `distinguish` near-miss wording from `print` and its total absence from `.save`; `copynode()` reducing `v(x)`→`x` and `i(vs)`→`vs#branch`; all five `name_eq()` callers (:286, :300, :324, :427, **:432** — the prompt named four, there are five); `OUTattributes()`'s two `strcmp`s dead because all seven in-tree callers pass `varName = NULL`; 113 decks in `tests/regression/case/` with `save` appearing only as `save alli` in the one pair; 23 decks in `casedist/` with no `save` at all; 56 `-case.cir` / 52 `-case-lower.cir` twins plus five named exceptions; and a prototype acceptance deck's RED shape.

**Claims from FINDINGS.md that did not hold up:** none failed to reproduce. Two were adjusted:

1. Finding 2's matrix **bolds the `distinguish` rc=1 cell as a defect alongside the `preserve` one**. It is not — under `distinguish` two spellings are two names, the lookup genuinely misses, and rc=1 is the correct answer. The issue states explicitly which cells the fix must change (the four `preserve` rc=1 cells) and which must not (the whole `distinguish` column, the whole `fold` column of deck routes).
2. Finding 2's "under `fold` the site only appears correct because the card was lowercased upstream" is correct but **understates it**. Measured, taking the fold away by typing `save v(MIDNODE)` at `ngspice -p` under `casemode=fold` kills the run too — and `ngspice-46`, with no casemode support at all, fails identically. So this is a default-mode, pre-casemode defect on every route the reader's fold does not cover, not a `preserve`-only one.

Two facts added that FINDINGS did not have: `outitf.c:324` compares two *simulator-side* names and must stay byte-exact — measured, a `tran` deck with a node named `Time` keeps that node under `preserve` only because of that exactness, and a blanket `cieq()` there would delete it; and `tests/lint/identity.baseline:197`/`:198` are the same class but unreachable (all callers pass NULL).

**Two line numbers in the prompt were off** and are corrected in the issue: `vec_name_eq()` is defined at `src/frontend/vectors.c:413` (`:402` is inside its comment block), and the fifth `name_eq()` caller at `:432` was not in the prompt's list.

## Adversarial check

- verdict: **NEEDS_WORK**
- exists: True · headings_ok: True · citations_ok: True · measurements_ok: False

### Problems raised

1. MISSING DECK (serious, reproducibility). Three quoted commands in Impact (file lines 113, 118, 131) run `source plain.cir`. There is no `plain.cir` in doc/claude/feedback/ngspice_upstream/repro/, nowhere else under doc/claude/feedback/, and `find` over the whole repo returns nothing. The Status paragraph asserts every number was re-measured against this build, but a reader cannot re-run those three commands as written. I reconstructed a plain deck (Vs In 0 DC 3 / Rl In MidNode 1k / Rg MidNode 0 3k / .end) and all three claims DO hold: `save v(MIDNODE)` under casemode=fold at the -p prompt gives `Error: no data saved for D.C. Operating point analysis; analysis not run` + `Name: const (constants)`; `save v(midnode)` gives `Name: op1 (Operating Point)` + `midnode : voltage, real, 1 long [default scale]`; `save v(MidNode)` fails the same way; and /usr/local/bin/ngspice (ngspice-46, no casemode) fails identically. So the evidence is true but not reproducible from the tree.

2. OVERSTATEMENT, contradicted by measurement. Impact opens: "Under `preserve` this does not merely lose a name -- it ends the run." That is only the all-saves-missed case (the run-level error is outitf.c:461-467). Measured on a partial miss, `.save v(In) v(midnode)` under casemode=preserve: rc=0, no error, no warning, nothing naming the token, and the plot simply comes back with `In : voltage, real, 1 long [default scale]` and no MidNode. Every cell of the issue's six-row matrix is a single-save deck, so the matrix never reaches the partial case -- which is the quieter and arguably worse consumer hazard, since a raw-reading client gets rc=0 and a well-formed plot minus one signal. doc/codex/issues/0057 criterion 2 names this case explicitly; 0056, which owns the comparison, does not mention it anywhere.

3. SCOPE OVERLAP with its own batch, with zero cross-references. 0056 contains no reference to 0057, 0058, 0059, 0060 or 0061 (grep for '005[6-9]|006[01]' returns nothing), while 0057 references 0056 twelve times and 0059 twice. Specifically: (a) the Impact paragraph "*It is silent about the token.*" plus acceptance criterion 3 are the entire subject of 0057 'A Failed .save Names No Token' -- same client report, finding 4 -- whose criteria 1/3/5 cover the same ground and whose criterion 8 says 'Sequenced after 0056'; 0056 claims that scope silently. (b) The paragraph "*It still writes a plausible raw.*" says finding 3 is 'filed separately if at all' -- it WAS filed, as 0059, dated the same day in the same batch, and 0059's Status defers the comparison back to 0056. That sentence is stale. (c) 'stderr of the failing run carries the experimental-mode banner twice' is precisely 0058's subject, uncited.

4. STATUS CLAIM OVERREACHES: 'nothing is copied from that report.' The numbers were indeed re-measured, but the Impact paragraph '*It hits hardest the tools that were already doing the right thing.*' is a near-verbatim rewrite of FINDINGS.md's 'Why this is the biggest migration hazard' paragraph -- compare 0056's 'A program that lower-cased names to cope with `fold` -- the natural thing to do, and what the reporting client did -- has every stored `.save` card become fatal the moment a user selects `preserve`' against FINDINGS.md:126 'Any tool that lower-cased names to cope with fold mode -- which is the natural thing to do, and what xschem did -- has every stored `.save` card become fatal the moment the user selects `preserve`.' The citations outitf.c:1338 and identity.baseline:196 are also already in FINDINGS' root-cause paragraph.

5. TWO MORE UNPINNED MEASUREMENTS (minor). Acceptance criterion 2's `Time`-node tran deck and the Root Cause candidate acceptance deck are quoted with output but given no path and are not committed anywhere. Criterion 2's deck is reconstructible only by inferring Rl==Rg from the phrase 'half of v(In)'. Both DO reproduce once reconstructed -- I got `In/Time/Vs#branch/time` at exactly '59 long' under preserve and `in/time/vs#branch` under fold, and the ASCII raw shows index 0 `time` = 0.0 (scale) and index 2 `Time` = 1.5 = half of v(In) = 3.0; the candidate deck gives `Error: RHS "v(midnode)" invalid` with no vprobe under preserve and `vprobe = 2.250000e+00` under fold.

6. QUOTE ELISION (minor). The raw-header block quotes `No. Variables: 12` immediately followed by `Variables:`, presenting them as contiguous. The actual file has `No. Points: 1` between them. No ellipsis marks the cut.

7. INCOMPLETE SURVEY BEHIND A BOLD CLAIM (minor). '**No deck anywhere saves a named vector by a spelling other than the stored one.**' is asserted after surveying only tests/regression/case/ and tests/regression/casedist/. It happens to be true tree-wide -- I grepped every .cir and .cmd under tests/ and the only save-with-a-name sites are postcoms-keyword-case.cmd:248/:269, device-keyword-case.cmd:362/:386, bsim3soipd/RampVg2.cir:15 and regression/misc/bugs-2.cir:10, none of which mis-spell a stored vector -- but the write-up does not show that survey. It also misses that tests/regression/pipe/ ALREADY has prompt-route save decks with case variation (`save SPEEDCHECK DELTACHECK v(out)`, `save @r1[I]`), which is directly relevant to criterion 6; criterion 6 instead cites shell-keyword-case.cmd, a less apt example.

8. UNRECORDED CONSEQUENCE (minor). doc/claude/decisions/0001-distinguish.md:314-316 states that after 0032 the frontend has 'two folded-key tables filtered by the same predicate and no vector-name matcher outside them'. 0056 falsifies that sentence -- name_eq() is exactly such a matcher -- but never notes that the decision record needs amending. Not a contradiction the issue creates, and its Class C classification, its evtprint.c:341 / evtshared.c:253 precedent (decision doc lines 357-358) and its criterion-2 argument for keeping outitf.c:324 byte-exact are all consistent with the record.
