# Stage 1 receipt — issue 0061

Issue file: `doc/codex/issues/0061-a-rawfile-option-line-reconfigures-the-next-netlist-read.md`

## Author receipt

`doc/codex/issues/0061-a-rawfile-option-line-reconfigures-the-next-netlist-read.md`

**Title:** Issue: A Rawfile `Option:` Line Reconfigures the Next Netlist Read

**Re-measurement:** every claim in the issue was re-measured on this build at `720c8743a`; all reproduced cleanly. Specifically re-run and pasted from actual output: the two-run `load hdr_option.raw` / bare `source mix.cir` comparison (`MidNode` vs `midnode`, plus the circuit-title fold); `$casemode` before/after `load` on both `build-ver_50/src/ngspice` and `/usr/local/bin/ngspice`; `Option: ngbehavior = hs` reaching `set_compat_mode()`; `Option: numdgt = 12` being inert (latched via `cp_usrset()`, `options.c:355`); `-D casemode=fold` beating the plot env; `set casemode=fold` refused (`Error: casemode is a read-only variable.`) and `unset casemode` refused (`Error: casemode is read-only.` + `cp_remvar: Internal Error: var 99`, which is `*varname` printed with `%d`); plot-scoped re-arming via `setplot`; round-trip through `raw_write()` re-emitting `Option: casemode = preserve`; `Command: echo …` executing via `cp_evloop()` on both binaries; `Command: set casemode=preserve` writing the global list and surviving the plot change; name byte-exact / value case-insensitive matrix; misplaced `Option:` before `Plotname:` being dropped. Every `file:line` citation was pinned by grep/sed; two were corrected from my first draft (`variable.c:699` not `:698`, `variable.c:1021` not `:1018`). Referenced test paths verified to exist; `grep -rln "Option:" tests/` is empty as stated.

**Prompt claim that needed adjustment:** the task text cited `cp_evloop()` at `src/frontend/rawfile.c:466` and the `option:` arm "around :479". Measured on this tree the `command:` arm opens at `:456` with `cp_evloop(s)` at `:470`, and the `option:` arm opens at `:472` with its two `cp_setparse()` writes at `:481`/`:483`. The issue uses the measured numbers.

**FINDINGS.md claim that did not hold up:** none of finding 1's *measurements* failed — `Casemode:` aborts the load on both builds, `Option:` after `Plotname:` loads clean on the unmodified `ngspice-46` and lands in the plot environment, and placed before `Plotname:` it prints `Error: misplaced Option: line` while the plot still loads. What did not survive is finding 1's *conclusion* from those measurements — "That is genuinely backward compatible … and it deletes the probe apparatus from every downstream tool." `Option:` is not inert metadata: it reconfigures the next netlist read, cannot be overridden by a later `set`, and propagates itself through `write`. That inversion is the whole content of issue 0061, and it matches the deferral already recorded for F1 in `doc/claude/batches/2026-08-12-casemode-client-feedback/LEDGER.md`.

**Established as asked:** the adjacent `Command:` path *is* deliberate and documented in-tree — `man/man1/ngsconvert.1:109` ("Any number of **Command:** lines may appear … and whenever the plot is loaded into **nutmeg** they will be executed"), with `Command:` also listed in the ASCII header grammar at `:89`. `Option:` appears in no man page and in no `doc/` file outside `doc/claude/`/`doc/codex/`. The issue keeps the two separable (acceptance criterion 4 names the `Command: set …` residue explicitly so a fix for `Option:` is not blocked on it).

**Resolution left as** "Unresolved." plus one line: a scoping rule — either the `Option:` arm refuses names that configure the reader, or `cp_getvar()`'s `plot_cur->pl_env` link stops being visible to `inp_readall()`'s two policy reads.

Nothing under `src/` or `tests/` was touched; scratch decks and generated `.raw` files live in the session scratchpad, and the regenerated `repro/*.raw` are gitignored.

## Adversarial check

- verdict: **NEEDS_WORK**
- exists: True · headings_ok: True · citations_ok: False · measurements_ok: False

### Problems raised

1. SUBSTANCE FIRST, so the flags below are not misread: the central finding is real and every behavioral output reproduced verbatim on this machine. `printf 'load hdr_option.raw\nsource mix.cir\nquit\n' | build-ver_50/src/ngspice -p -n` gave `In` / `MidNode` / `Vs#branch` and `Circuit: * mixed-case net probe -- the net is spelled MidNode`; without the load it gave `in` / `midnode` / `vs#branch` and `... spelled midnode`. Properties 1-5 all reproduced exactly as quoted (`Note: Compatibility modes selected: hs` vs `Note: No compatibility mode selected!`; both numdgt runs `v(in) = 3.000000e+00`; `dollar-casemode=fold` + `midnode`; `Error: casemode is a read-only variable.` / `now=preserve` / `MidNode`; `Error: casemode is read-only.` + `cp_remvar: Internal Error: var 99`; `after-load=preserve` / `after-source=` / `back-on-op1=preserve`; the rt.raw round trip with `Option: casemode = preserve` re-spaced after `No. Points: 1`, and `MidNode` on reload). Both `$casemode` runs gave `before=` / `after=preserve` on build-ver_50 AND on /usr/local/bin/ngspice (ngspice-46). Both `Command:` runs printed `COMMAND-LINE-EXECUTED`, and the cmdset run gave `MidNode` + `after-source=preserve`. The Impact table reproduced: `Option: CaseMode=`/`Option: CASEMODE=` both fold, only `casemode` preserves; `PRESERVE` and `Preserve` both preserve. `grep -rln "Option:" tests/` is empty. The misplaced-line claim reproduced (option dropped, plot still loads, `cm=` empty, deck folds).

2. CITATION MISATTRIBUTION (the one real citation defect). Property 3 says "`cp_variablesubst()` does the same (`variable.c:1005` before its `cp_enqvar()` fallback at `:1021`)". `cp_variablesubst()` is variable.c:846-903 and does not contain either line. Lines 1005 and 1021 are inside `vareval()`, which starts at variable.c:908. The mechanism claim is still true (cp_variablesubst calls vareval at :866, vareval walks `variables` at :1005 then falls back to cp_enqvar at :1021, and cp_enqvar scans `plot_cur->pl_env` at options.c:58-64) - but a reader opening the cited line lands in a different function than the one named.

3. GREP EVIDENCE DOES NOT MATCH THE QUOTED CLAIM. The issue asserts "`rawfile.c:481`/`:483` is the **only** writer of `pl_env` in the tree - a `grep -rn \"pl_env *=\" src/` finds nothing else but two `killplot()` debug `printf`s". Re-run, that grep prints exactly three lines: rawfile.c:483 and postcoms.c:1128, :1157. It does NOT print :481 (that line is `vv->va_next = cp_setparse(wl);`, which the pattern cannot match), so the quoted command does not produce the quoted conclusion. The pattern also cannot see indirect writes, and there is one: `cp_remvar()` sets `p = &plot_cur->pl_env` (variable.c:600) and writes `*p = v->va_next` (variable.c:631) in its `US_OK` arm. That arm is unreachable for a pl_env name because `cp_usrset()` returns US_READONLY (options.c:414-417) - which is the issue's own property 4 - so the conclusion survives, but the stated proof does not establish it.

4. QUOTED OUTPUT THAT DOES NOT REPRODUCE BYTE-EXACTLY (cosmetic, one field). Both header pastes carry `Date: Wed Aug 12 23:46:22  2026`. The committed `repro/hdr_option.raw` carries `Date: Wed Aug 12 23:56:10  2026`, and so does the `sed -n '1,9p' rt.raw` output I got. Every other byte of both pastes matched. `run_all.sh` does `rm -f ./*.raw` and regenerates, so the repro set was rebuilt ~10 minutes after the issue was written; the stamp is the only casualty. Nothing behavioral diverges.

5. REPRO ARTIFACTS MOSTLY ABSENT, for a write-up whose weight is 'measured'. Of the six inputs the quoted commands use, only `hdr_option.raw` exists in `doc/claude/feedback/ngspice_upstream/repro/`. `mix.cir`, `hdr_ngb.raw`, `hdr_numdgt.raw`, `hdr_cmd.raw` and `hdr_cmdset.raw` exist nowhere in the tree and `run_all.sh` does not generate them (it generates `hdr_newkey.raw` and `hdr_option.raw` only). mix.cir's text is given inline and hdr_ngb/hdr_numdgt/hdr_cmd are each described by their one differing line, so I could rebuild those; `hdr_cmdset.raw`'s content is never stated anywhere - I had to guess `Command: set casemode=preserve` (it reproduces). The whole repro dir is untracked (`git ls-files` there returns nothing), so none of this is recoverable from a checkout.

6. OFF-BY-ONE CITATIONS (minor, all land in the right construct). Impact table cites `variable.c:704` for "`eq()` = `strcmp`"; :704 is the `for (v = plot_cur->pl_env; ...)` header and the `eq()` call is :705. Property 2 says "`options.c:355` copies it into `cp_numdgt`"; :355 is the `else if (eqc(var->va_name, "numdgt"))` test and the copies are :357/:359/:361. Property 5's `postcoms.c:1157` is described as a `killplot()` printf but sits in `destroy_const_plot()` (starts :1136) - only the printf's text says killplot; :1128 is genuinely in killplot.

7. OVERSTATEMENT, tempered later but not at the point of claim. Summary: "So every `cp_getvar()` consumer in the tree can be answered by a line in a file the user merely *loaded*." Only consumers whose name is not already in `variables` or `cp_usrvars()` - which is the issue's own property 3, two pages down.

8. OVERSTATEMENT, 'exactly'. Property 2 concludes "the reachable set is exactly 'names some site passes to `cp_getvar()` at the moment it needs them'". The set is strictly larger: `$var` substitution reaches pl_env through `cp_enqvar()` (options.c:58-64), not through cp_getvar's chain, and `cp_vprint()` (the `set` listing) reads pl_env at variable.c:1125 and :1147. The issue's own acceptance criterion 3 relies on that readback existing, so 'exactly' contradicts criterion 3.

9. WEAK EVIDENCE AS PRESENTED (conclusion verified true by me, but not by what is shown). Property 2's numdgt paste is two identical `v(in) = 3.000000e+00` lines with no control, so as written it cannot distinguish 'Option: numdgt does nothing' from 'numdgt never changes this line'. I supplied the missing control: `set numdgt=12` after the load gives `v(in) = 3.000000000000e+00`, and `grep -rn 'cp_getvar("numdgt"' src/` is empty, so the claim holds - but the issue should show the control.

10. MINOR UNDER-QUOTE. Root Cause says the comment at inpcom.c:1079 "names `-D` and `.spiceinit` as the writers it considered". The comment also names libngspice's `ngSpice_Command("set casemode=...")` as a third writer. Does not damage the argument (a load is later than that one too).

11. SCOPE, borderline but I would not call it stray. The ~55-line `Command:` section is adjacent-issue material, but it is load-bearing for acceptance criterion 4, it explicitly declines to ask for any change there, and it explains why the two are separable. NOT a duplicate of 0056-0060: 0060 is the only sibling that mentions `Option:` and it explicitly hands the mechanism to 0061 ('The mechanism is `doc/codex/issues/0061` and belongs there'), keeping only the one measurement that bears on `$casemode`. No overlap with 0056/0057/0058/0059 (none contains the string `Option:`).

12. NO CONTRADICTIONS FOUND with `doc/claude/decisions/0001-distinguish.md` or with any closed issue. The 'in none of the three classes, it is a scoping defect' framing matches how 0001 decision 6 treats 0029(a) ('not an identifier comparison and so is not in its table'). The Class B claim for the value comparison matches 0001's 'Keywords are case-insensitive in all modes, per the spec's compatibility contract point 2'. 0048 is Open, and the 'name is 0048's territory' cross-reference is accurate; the `tests/regression/pipe/` rationale it invokes is really in 0048:108-110. `tests/regression/casedist/vector-rawfile-scale-case.cir` exists and is genuinely the 'deck writes the hand-built rawfile it then loads' precedent. The LEDGER F1 deferral is at LEDGER.md:41-43 and reads as claimed. `man/man1/ngsconvert.1:109` and `:89` are both exact.

13. ALL OTHER CITATIONS VERIFIED EXACT: rawfile.c:456, :470, :472, :481, :483, :485 (all inside raw_read, 328-770) and :132-:148 (inside raw_write, 42-295); variable.c:694, :699, :703, :188, :644, :646 (`*varname` printed with `%d`, and 'c' is 99); inpcom.c:1079, :1085, :1091, :1096, :1176, :1178; options.c:417; postcoms.c:107, :112, :1128; aspice.c:238; main.c:1612 (confirmed inside the `#else /* ~ SIMULATOR */` at :1600). One transcription liberty: the `Command:` code block silently drops the comment at rawfile.c:457 with no ellipsis, though the line labels stay correct.
