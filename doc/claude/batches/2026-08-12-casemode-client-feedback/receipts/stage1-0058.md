# Stage 1 receipt — issue 0058

Issue file: `doc/codex/issues/0058-casemode-diagnostics-fire-once-per-file-read.md`

## Author receipt

**Path:** `/home/qflow/dev/ngspice_test/doc/codex/issues/0058-casemode-diagnostics-fire-once-per-file-read.md` (375 lines; headings in the required order; `src/` and `tests/` untouched)

**Title:** `Issue: The casemode Diagnostics Fire Once Per Input File Read, Not Once Per Run`

**Every claim re-measured cleanly — yes.** Nothing from FINDINGS.md finding 7 failed to hold up. The three counts reproduced exactly: `SPICE_SCRIPTS` at a `spinit`-less dir + `-n` → 1, normal `-n` → 2, `repro/count/deck.cir` with its `.spiceinit` and no `-n` → 3; the `distinguish` banner behaves identically (1/2/3) at 320 bytes per emission. Attribution of the third line to the `.spiceinit` confirmed by adding `-n` (drops to 2).

**What the issue adds beyond the finding** (all measured, all new):

- **Further reads that push it higher.** `.include` and `.lib` do **not** (2 each, unchanged) — they recurse inside `inp_read()`, below `inp_readall()`. Four things do: `ngspice -p` grows by one per `source` (2 / 3 / 4 for zero / one / two sources); a `.control` `source` in batch (3); `altermod @mod file <f>` via `src/frontend/device.c:1598` (3); and the XSPICE auto-bridge's synthetic deck via `src/xspice/evt/evtcheck_nodes.c:232` (3 with a bridge, 2 without — `expand_deck()` runs only when a bridge card was generated). Also `-p` emits one announcement with no user file behind it at all: `main.c:1073` sets `istty`, so `main.c:1423` skips the stdin append and `main.c:1503` sources an empty temp file.
- **The count is not monotone in how experimental the run was.** A run entirely under `distinguish` prints the banner **2** times; a run that starts in `fold` and switches to `distinguish` for a sourced deck prints it **1**. Backed by a deck showing two modes live in one process (`midnode` from deck 1, `MidNode` from deck 2).
- **`make check` already prints ~80 copies (~25 kB).** `tests/regression/casedist/Makefile.am:23` and `tests/xspice/casedist/Makefile.am:28` run 23 + 17 decks under `-D casemode=distinguish`, 2 banners each, invisible because `tests/bin/check.sh:29` captures stdout only.
- **Both `fprintf`s are literal `stderr`, so no deck can guard a fix.** Measured with decision `0006`'s `echo`+`source`+`>&` shape: 0 hits in the capture; `vec_warn_case_near_miss()` (`vectors.c:464`, `cp_err` at `:478`) *is* captured by the same redirect.

**Root-cause verdict asked for:** gating on `comfile` is **necessary but not sufficient**, and the issue says so with the reason. It fixes the 1/2/3 table (both init files are the `comfile == TRUE` readers) but leaves four `comfile == FALSE` readers; and `print_compat_mode()` — the tree's existing example of exactly that split, `inpcom.c:1176` vs `:1214` — is itself measured at 2 for a run that sources a second deck. The mode must be **latched on change**, because a single process legitimately holds two modes. Also measured: the call itself cannot be gated — a `.spiceinit` containing `set MixedVar = 1` stores `mixedvar` under `fold` and `MixedVar` under `preserve`, so command-file reads genuinely need the mode established.

**Class:** none of decision `0001` decision 3's Class A/B/C applies — that taxonomy classifies name *comparisons* and this is an emission. Stated explicitly and pointed instead at decision 2 and its 2026-08-11 `cp_err` correction.

**Resolution:** "Unresolved." plus one sentence on the fix shape (split like `set_compat_mode()`/`print_compat_mode()`, latch behind a file-static last-announced mode cleared on shared-build reset, move both writes to `cp_err` in the same commit).

## Adversarial check

- verdict: **NEEDS_WORK**
- exists: True · headings_ok: True · citations_ok: False · measurements_ok: False

### Problems raised

1. MEASUREMENT DOES NOT REPRODUCE (Impact 2). The issue says the casedist suites 'run all 23 + 17 decks under -D casemode=distinguish, and each deck emits the banner twice ... so one make check prints roughly 80 copies, about 25 kB'. Measured on 720c8743a with build-ver_50/src/ngspice, same flag, harness-equivalent env: tests/regression/casedist totals 62 emissions, tests/xspice/casedist totals 51 -> 113 copies, ~36 kB, not ~80/~25 kB. 13 of the 40 decks emit 3-6, not 2 (bsource-node-case-report 6, sens-node-case-report 6, subckt-formal-pin-case 5, vector-let-report 5, vector-probe-report 4, all eleven auto-bridge-vcc/family decks 3, auto-bridge-node-case-report 6, event-node-case-report 6). Spot-checked again from build-ver_50/tests/regression/casedist with ngspice_vpath set: same numbers. This is also an internal contradiction: the issue's own Summary establishes that each 'source' and each generated auto-bridge adds a read, and those two dirs are exactly where the sourcing and bridging decks live.

2. CITATION OFF BY FIVE, used three times. 'src/main.c:1503 - the command-line deck, or in -p mode the temp file even when it is empty' (call-site table), and again twice in the -p paragraph ('src/main.c:1503 sources it anyway', 'by the table above it is the only caller reachable'). src/main.c:1503 is 'if (tempfile && (!err || !ft_batchmode)) {'. The inp_spsource() call is at src/main.c:1508. The substance of the claim is correct (verified: -p -n with SPICE_SCRIPTS at a spinit-less dir still prints 1, preceded by the 'can't find the initialization file spinit' warning), but the line is wrong.

3. THE TABLE'S EXHAUSTIVENESS CLAIM IS FALSE AS WORDED. It is headed 'Every inp_readall() call site in the tree, with the comfile/intfile arguments it passes'. Four of its seven rows are inp_spsource() calls, not inp_readall() calls: cpitf.c:291, inp.c:1985, inp.c:1995, main.c:1503(->1508). The one inp_readall() call all four of them actually reach, src/frontend/inp.c:532, is never named anywhere in the issue. The genuine call sites are inp.c:532, nutinp.c:38, device.c:1598, evtcheck_nodes.c:232 - only the last three appear. A second inp_spsource() reader, src/main.c:1524 (inp_spsource(circuit_file, FALSE, NULL, FALSE), the -c/batch-fallback path), is omitted from a table claiming to be complete.

4. QUOTED OUTPUT NOT PRODUCIBLE FROM THE DECK AS SHOWN (Impact 3). The switcher.cir block printed in the issue contains exactly one 'display', on the line before 'source mid.cir'. mid.cir's contents are never shown. Reconstructed and measured: with a mid.cir that has no display of its own, the output of the quoted grep is only 'FIRST-DECK' and one 'midnode' line - the 'MidNode' line requires a second, unshown 'display' inside mid.cir. Consequently the accompanying sentence 'Two modes, one process, one plot list' is wrong: the two lines come from two different displays in two different plots (op1 and op2), not one plot list. The underlying claim (the mode really does change mid-process) does reproduce; the quoted evidence for it does not, as printed.

5. SUPPORTING DECKS ARE NEITHER COMMITTED NOR SHOWN, so most of the issue cannot be re-run by a reader. Absent from doc/claude/feedback/ngspice_upstream/repro/ and unquoted in the issue: inc_deck.cir, lib_deck.cir, plain.cir, am.cir (plus its .mod), switcher.cir's circuit body, mid.cir, switcher_d.cir, d.cir and the '.spiceinit' beside it, outer.cir, and the wrapper that produces cap.txt/c2.txt. Only three of the issue's ~20 quoted commands use committed decks (divider.cir, count/deck.cir, case_collision.cir). Three quoted commands are also not literally runnable: the three -p commands end in 'grep -c ...' with a literal ellipsis, and the auto-bridge command reads '--batch <same deck, bridge removed>'. I rebuilt all of them from the prose and every one reproduced, but that is my reconstruction, not the issue's.

6. THE PROPOSED FIX RULE DOES NOT DO WHAT ROOT CAUSE SAYS IT DOES, and would silently lose a case that works today. Under a misspelled value, the mode being established is NG_CASE_FOLD (inpcom.c:1089; the else arm at :1125 changes nothing), byte-identical to a plain fold run. So 'remember the mode last announced, and announce only when the mode being established differs from it' cannot separate 'bogus' from 'fold'. It yields the promised 1/1/1 only if the latch is initialised to a sentinel that is none of the three modes - which the issue never states - and even then: measured today, 'ngspice -b -n -D casemode=fold switcher_b.cir' where the .control block does 'set casemode=bogus' then 'source mid.cir' prints "Warning: unknown casemode 'bogus', using 'fold'" exactly once; under the proposed rule the first read has already latched FOLD, so the mid-run misspelling would print zero times. The sentence 'The rule that satisfies every measurement in this issue is...' is therefore not established, and Acceptance Criterion 1's '1 / 1 / 1' is asserted, not derived.

7. MINOR - Status contradicts Summary. Status: 'Visible in two of the three modes - distinguish, and any run whose casemode value is misspelled.' A misspelled value is not one of the three modes. Summary states it correctly ('in two of its four arms it announces it').

8. MINOR - 'Pre-existing since set_case_mode() was added' is true of the unknown-value warning only. git log -S shows set_case_mode() and the 'unknown casemode' fprintf arrived in 424ebaf75; the 'is experimental' banner arrived later, in d06326d05.

9. MINOR - scope stretch on the governing decision. 'The decision that governs it is decision 2, the diagnostic rule' - decision 2 of doc/claude/decisions/0001-distinguish.md is titled and scoped to 'the diagnostic for a case near-miss'. A mode banner is an emission about the mode, not a near-miss report. The cp_err-not-stderr rule is quoted accurately from that decision's second 2026-08-11 correction (verified at 0001-distinguish.md:143-160), and the 0036 characterisation is accurate (0036's Resolution did move three diagnostics from literal stderr to cp_err), but applying decision 2 to this emission is an extension the decision does not itself make.

10. MINOR - batch hygiene. 0058 and 0060 dissect the same function and cite the same three lines (inpcom.c:1034, :1085, :1178) and neither cross-references the other; in fact none of 0056-0061 cross-references any other. Worse, 0060's Acceptance Criterion 2 states that 'a source or an .include from inside a .control block' is an inp_readall(), which directly contradicts 0058's measured '.include and .lib do not'. 0058 is the one that measured it and is right (I reproduced 2 for both an .include deck and a .lib deck), but the batch needs reconciling.
