# Stage 1 receipt — issue 0057

Issue file: `doc/codex/issues/0057-a-failed-save-names-no-token.md`

## Author receipt

`/home/qflow/dev/ngspice_test/doc/codex/issues/0057-a-failed-save-names-no-token.md`

**Title:** Issue: A Failed `.save` Names No Token

**Re-measurement:** every claim in the issue was re-run at `720c8743a` against `build-ver_50/src/ngspice` and `/usr/local/bin/ngspice`; all output pasted is what I actually got. Nothing was copied from FINDINGS.

**FINDINGS claims that did not hold up as stated:**

1. **Finding 4's framing as a `casemode` consequence does not hold.** The defect is mode independent and pre-dates `casemode`. Measured: `.save v(nosuchnode)` on stock `ngspice-46` (no flag, no feature) gives the identical `Error: no data saved…` with `grep -ic nosuchnode` = 0 on both streams. Under this build the same silence occurs in `fold`, `preserve` and `distinguish`. `preserve` only makes it easy to reach. The issue says so explicitly and adjusts the acceptance criteria to "all three modes, and for a name with no case variant".

2. **"Nothing in the output names the offending token" is true for `.save` but not for the save machinery.** A near-token-naming diagnostic already exists *in the same function*, `src/frontend/outitf.c:419` `Warning: can't parse '%s': ignored`, gated on `saves[i].analysis` at `:418`. `.save` cards reach it with `analysis == NULL` (`dotcards.c:74` → `com_save()` → `settrace(…, NULL)`), so it is suppressed; `.print`/`.plot`/`.four`/`.op`/`.tf`/`.meas`-derived saves set the field and do fire it. Measured contrast, same net, same spelling, same build: `.print dc v(midnode)` under `preserve` prints `Warning: can't parse 'midnode': ignored`, `.save v(midnode)` prints nothing.

3. **"rc is not available" understates it.** In the *partial* miss (one `.save` resolves, one does not) rc is **0**, stderr is **empty**, the analysis runs and the raw is written without the signal — measured with `display`, which lists only `In`. FINDINGS only covers the all-saves-missed case. This is added as the more severe of the two failure shapes.

Other notes: the near-miss message is at `src/frontend/vectors.c:478-481` (function at `:464`), not `:480` as the task brief had it. Five emitters of the message family enumerated with line numbers, and the reason `.save` is not among them measured (`grep -c "no node named"` = 0 under `distinguish`, and `plotInit()` at `outitf.c:479` runs *after* the `return E_NOTFOUND` at `:466`, so no lookup table exists for `vec_warn_case_near_miss()`). Guardability confirmed: `op >& cap.txt` plus `fopen`/`fread`/`strstr` recovers the error inside the deck (a `shell cat` of the same capture reads empty). Only that one file was written; `src/` and `tests/` untouched.

## Adversarial check

- verdict: **NEEDS_WORK**
- exists: True · headings_ok: True · citations_ok: False · measurements_ok: False

### Problems raised

1. Duplicates a prescription already owned by 0056 in the same batch, without saying so. 0056 criterion 3 ('The distinguish half of decision 2 ... is implemented or recorded as deliberately not. .save is a resolution, it misses beside a case variant, and it says nothing. vec_warn_case_near_miss()'s wording is the one to reuse') and 0056's Resolution ('plus the decision-2 near-miss warning on cp_err from the Pass 1 miss') already claim exactly what 0057 criterion 3 and 0057's Resolution ask for. 0057's 'Relationship to 0056' section argues at length that the two are independent and even sequences itself after 0056, yet never mentions that 0056 as written already lands criterion 3 -- if 0056 ships as specified, 0057 criterion 3 is dead on arrival. One of the two must give up the near-miss half.

2. Criterion 1 attributes to decision 2 a rule decision 2 does not state, and skips the precedent that actually supports it. doc/claude/decisions/0001-distinguish.md:76-78 conditions the warning on '...the resolution fails, AND a name differing only in case exists', and lines 93-96 explicitly reject turning an unresolved name into an error. Criterion 1 demands a report 'in all three modes ... and for a name with no case variant as well as one with' and says 'decision 2's rule places this'. It does not. The mode-independent, no-twin half is doc/codex/issues/0028 + doc/claude/decisions/0008-undefined-node-diagnostic.md (referenced but no card defines it), which the write-up never cites once -- grep for 0028/0008 in the file returns nothing. Not a contradiction of a closed record, but the strongest half of the issue is grounded on the wrong one.

3. The six-row asymmetry table does not reproduce from the deck it describes. 'Measured on a divider with .dc Vs 1 3 1, MidNode as the net, and one card varied' -- built exactly that way, the '.save v(midnode) | fold | 0 | n/a - resolves' row measures rc=1 (stderr: 'Error: incomplete or empty netlist ... no simulations run!'), because a .dc card with no .print never runs in batch. All six rows reproduce only after adding an unstated '.control / run / write / .endc' block: c_smid fold rc=0, c_smid preserve rc=1 no token, c_snos fold rc=1 no token, c_pmid fold rc=0, c_pmid preserve rc=1 'Warning: can't parse 'midnode': ignored', c_pnos fold rc=1 with token. The table's substance holds; its stated method does not.

4. Two quoted command blocks cannot be re-run as written: save_absent.cir (the baseline block) and t3.cir (the partial-miss block) do not exist in doc/claude/feedback/ngspice_upstream/repro/ and their contents are never given, while Status claims every measurement was re-run 'from doc/claude/feedback/ngspice_upstream/repro/'. Both reproduce exactly once reconstructed from the prose, but a reader cannot repeat them. (Separately, the whole doc/claude/feedback/ tree is untracked -- git ls-files doc/claude/feedback is empty -- so no cited repro deck is committed; that hits the whole 0056-0061 batch, not 0057 alone.)

5. Citation slips, all in the right function but not at the line claimed: (a) 'the error and the return E_NOTFOUND are :465-466' -- the fprintf spans 465-466 and the return E_NOTFOUND is at outitf.c:467; the derived 'thirteen lines earlier' is 14 lines from :465. (b) 'the array OUTpBeginPlot() reads at src/frontend/outitf.c:222' -- :222 is inside the static helper beginPlot() (declared :48, defined :168); OUTpBeginPlot() is :110 and merely calls it at :124. 0056 gets this right ('all inside beginPlot()'). (c) 'from findvec() (:245) and vec_remove() (:658)' -- the calls are at vectors.c:246 and :661; :245/:658 are the guard ifs. Everything else resolves: dotcards.c:58/74/138/147/156/159, breakp2.c:34/42/126/161, outitf.c:286/300/411/417-422/418/419/465/1338/1358, vectors.c:464/478-481, inpsymt.c:143/165/169, subckt.c:1708/1722, evtcheck_nodes.c:910/935, evttermi.c:222/240, com_measure2.c:355/363, tests/lint/identity.baseline:196, tests/regression/casedist/vector-unlet-report.cir. The quoted C block at :417-422 is byte-exact, and grep confirms exactly five 'differs only in case' emitters as the table claims.

6. Overstated deck-coverage claim: 'No deck under tests/regression/case* or tests/regression/casedist/ uses .save at all.' tests/regression/case/save-alli-case.cir:34 and save-alli-case-lower.cir:13 use the control-language 'save alli', which the issue itself argues is 'the same path and the same silence'; and tests/regression/case/savecurrents-case.cir (plus its lower twin) drives inp_savecurrents() to emit '.save @dev[...]' cards, as its own header comment says. Worse for the framing, save-alli-case.cir:13 already documents the exact run-level line this issue is about ('Error: no data saved for D.C. Operating point analysis; analysis not run') and notes it is uncapturable by check.sh. The narrow claim -- this defect is unguarded -- survives; the sentence as written does not.

7. Internal inconsistency in the Root Cause: 'it is none of the three places a near-miss can currently be reported from', immediately after a table listing five emitters over at least four distinct search sets (frontend vector table, parser term symtab, subckt formal-pin table, CKTnodes/event node list). Also 'the table enumerates identifier comparisons' as the reason .save is absent from decision 2's table is wrong -- decision 2's table enumerates sites and rules (mkvnode, auto-bridge, vec_remove from com_unlet, rawfile scale=, vec_get probes, INPtermInsert, .model/.subckt/.global); it is decision 3's table that enumerates comparisons.

8. Convention deviation: a seventh section, '## Relationship to 0056 and 0059', sits between Root Cause and Acceptance Criteria. The six required headings are present and in order, but 0020, 0049, 0051, 0053, 0054, 0056 and 0059 all carry exactly six. The material belongs in Impact or Acceptance Criteria under the repo's structure.

9. Minor quoted-output trim: the partial-miss block's 'sed -n '/vectors currently active/,$p' t3.out' ends at the 'In ... [default scale]' line; my reconstruction emits a further 'Note: Simulation executed from .control section' line at the end of that same range. Either the deck differed or the capture was silently trimmed.

10. Scope overlap with 0059: the Impact's ls -la / head -c 300 / load+display block is 0059's own evidence (0059 lines 46-80 carry the same 570-byte header and the same twelve constants). It is attributed to 0059 and used to argue the combined blast radius, so it is defensible, but the load+display half adds nothing 0059 does not already establish.
