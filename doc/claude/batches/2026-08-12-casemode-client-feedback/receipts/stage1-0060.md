# Stage 1 receipt — issue 0060

Issue file: `doc/codex/issues/0060-casemode-variable-reports-the-request-not-the-effect.md`

## Author receipt

`/home/qflow/dev/ngspice_test/doc/codex/issues/0060-casemode-variable-reports-the-request-not-the-effect.md`

**Title:** Issue: `$casemode` Reports the Request, Not the Effect

**Re-measurement:** every claim in the issue was re-run at `720c8743a` against `/home/qflow/dev/ngspice_test/build-ver_50/src/ngspice` and `/usr/local/bin/ngspice`, and every quoted transcript is pasted from the run I did, not from FINDINGS.md. Nothing in the issue is copied. All of finding 8's claims held:

- `late_set.cir` under the case-capable build: `in` / `midnode` / `vs#branch`, `casemode-is preserve`, rc=0, stderr empty.
- Same deck-with-the-`set`-removed under both binaries with `-D casemode=preserve`: `In`/`MidNode` vs `in`/`midnode`, both echo `preserve`, both rc=0, baseline stderr empty.
- Finding 5 is confirmed **not** an instance: with `repro/spiceinit/.spiceinit`, the names fold and `$casemode` answers `fold`. Stated as such in the issue.

**Claims that did not hold up / needed adjustment:** none from FINDINGS.md finding 8. Two adjacent measurements had to be qualified rather than corrected:

1. Finding 7's "3 warnings with a `.spiceinit` beside the deck" only reproduces when that `.spiceinit` does **not** itself set `casemode`. With `repro/spiceinit/.spiceinit` (which sets `casemode=fold`) the count is 2, because the init file replaces the bogus value. With a neutral `.spiceinit` the counts are exactly 1 / 2 / 3 as FINDINGS says. The issue cites the fact, not the table.
2. FINDINGS 8's `-p` example only shows the "request is later than the latch" direction. I added three measured shapes it does not carry: (a) `-p -n` with `set casemode=distinguish` typed at the prompt — `$casemode` says `distinguish`, no experimental banner, `print abc` finds `ABC`; (b) the reverse — `-D casemode=distinguish` then `set casemode=fold`, where `echo $casemode` answers `fold` two lines above ngspice's own `Warning: ... (casemode=distinguish)`; (c) the default, where the effective mode is `fold` and the read is `Error: casemode: no such variable.`

**Extra in-tree evidence found and measured, not in FINDINGS.md:** `tests/regression/case/harness-alive.cir` produces byte-identical output (`CASEMODE-IS preserve`) from `/usr/local/bin/ngspice` and the case-capable build, so the directory's liveness probe passes on a binary with no `casemode` at all; and `tests/regression/casedist/vector-pyplot-probe-report.cir:62-67` gates assertions on `strcmp cm "$casemode" distinguish`.

**Scope:** one file written. `src/` and `tests/` untouched (`git status` shows no modified tracked files). `doc/claude/feedback/ngspice_upstream/repro/` artifacts were restored by re-running `run_all.sh` (rc=0) after my measurements; all scratch decks were written under the scratchpad. Resolution is "Unresolved." plus one line on the fix shape.

## Adversarial check

- verdict: **NEEDS_WORK**
- exists: True · headings_ok: True · citations_ok: False · measurements_ok: True

### Problems raised

1. FALSE CLAIM, refuted by measurement (Impact, line 171-172): "For the mode that has actually shipped there is no probe of any kind, and this variable is the thing that looks like one." The guide's OWN replacement for the sentence the issue quotes at guide:47 is three lines below it (doc/claude/casemode-distinguish-guide.md:52-62) and is a THREE-WAY deck probe that reads `preserve`. Measured on build-ver_50/src/ngspice with a deck whose .control holds `let CaseProbe = 1` / `let caseprobe = 2` / `print CaseProbe caseprobe`: -D casemode=fold -> `caseprobe = 2.000000e+00` twice; -D casemode=preserve -> `CaseProbe = 2.000000e+00` and `caseprobe = 2.000000e+00`; -D casemode=distinguish -> `CaseProbe = 1.000000e+00` and `caseprobe = 2.000000e+00`; no flag -> the fold shape; and /usr/local/bin/ngspice -D casemode=preserve -> the fold shape, so it separates the two binaries too. The issue attributes "the guide's replacement" to §9 only (the pipe-mode identity probe, which genuinely cannot see preserve, and which criterion 4 describes correctly as "identity-based"), and never mentions §2. Guide:222-224 explicitly says "§2's three-way table works only from inside a deck", so the issue contradicts the document it cites as its authority.

2. OVERSTATEMENT, contradicted by the cited guide (Impact, lines 198-201): "A consumer of ngspice output cannot learn how the run cased its names. A schematic editor that generated the deck, launched the run and is now reading names back has, today, no question it can ask." A deck-generating client can splice the §2 probe into the .control block of the deck it is already generating and read the effective mode for that exact run off stdout (measured above). guide:234-248 ("What actually changes for a schematic tool: the raw file") and guide:224-226 ("To confirm `preserve` from a client, run one throwaway deck and look at the raw file") are the tree's documented answer to precisely this consumer. The true, narrower claim is that no probe exists for a live session that reads no deck — which is what criterion 3's second and third bullets are actually about.

3. FALSE CROSS-REFERENCE (Impact, lines 209-215): "Reachable from three routes and not from a deck's own card. The `.control` route (`late_set.cir` above), the interactive prompt and `ngspice -p`, and `ngSpice_Command(...)` — ... These are the same three routes `doc/codex/issues/0048` and `0033` name, for the same reason." They are not. 0048:69-73 reads "Not reachable from a deck. The reader lower-cases a `.control` line before `com_set()` sees it ... It is reachable from the interactive prompt, from `ngspice -p` on stdin, and from `ngSpice_Command(\"set ...\")`", and 0033:45-46 names the same three. Both EXCLUDE the `.control` route, which is 0060's first route and its headline measurement. The reason differs too: 0048/0033's reason is the reader folding a mixed-case variable NAME; 0060's is the latch having already fired. The sentence is wrong in both halves. (Criterion 7, line 328-331, cites 0048 correctly — only this Impact sentence is wrong.) The bolded lead "and not from a deck's own card" also reads oddly when route 1 is a block inside a deck.

4. FALSE CLAIM, refuted by measurement and by the issue's own criterion 6 (Summary, lines 34-37): "`casemode` has no arm in `cp_usrset()` ... so every `set casemode=…` is `US_OK`: recorded in the variable list, no side effect." The grep is correct (no `casemode` in src/frontend/options.c, rc=1) but the conclusion is not: cp_usrset falls through to src/frontend/options.c:414-417, which returns US_READONLY for ANY name found in `plot_cur->pl_env`. Criterion 6 itself establishes that a rawfile `Option:` line puts `casemode` into that environment. Measured in doc/claude/feedback/ngspice_upstream/repro/: `printf 'load hdr_option.raw\nset casemode=fold\necho after $casemode\nquit\n' | build-ver_50/src/ngspice -p -n` -> `ngspice 10002 -> set casemode=fold` / `Error: casemode is a read-only variable.` / `after preserve`. So `set casemode=` is reachably US_READONLY, not "every ... US_OK".

5. FALSE CENSUS, contradicted by the issue's own grep four paragraphs later (Impact, line 174): "Two committed decks consume `$casemode`". Four do. Measured: `grep -rn '\$casemode' tests/` -> tests/regression/case/harness-alive.cir:14, tests/xspice/casedist/harness-alive.cir:23, tests/regression/casedist/harness-alive.cir:19, tests/regression/casedist/vector-pyplot-probe-report.cir:64 — four lines, which is the count the issue itself reports at line 218-219. The two omitted decks are not incidental: tests/regression/casedist/harness-alive.out and tests/xspice/casedist/harness-alive.out both assert `CASEMODE-IS distinguish` and carry exactly the weakness the issue attributes to the `case/` one, so omitting them understates the issue's own case while making a checkable statement that is false.

6. MISATTRIBUTED DECISION CITATION (Root Cause, line 255-256): "they are language keywords, decision 3's 'Not withdrawn' list." The "Not withdrawn" list is doc/claude/decisions/0001-distinguish.md:482, inside "## Decision 5 — migration note: what `preserve` guarantees that `distinguish` withdraws" (starts at :452); decision 3 runs :166-413 and has no such list (`grep -n 'Not withdrawn'` returns exactly one hit, :482). Separately, the list names ".TRAN, PULSE, PARAMS:, UIC, %VD, .model type names, B-source function names" — the `casemode` mode values are not on it, so their classification is the issue's own inference presented as the decision record's.

7. OFF-BY-TWO CITATION (Root Cause, line 249): "Class A's `inp_case_exact_ids()` (`:1062`)". `bool inp_case_exact_ids(void)` is at src/frontend/inpcom.c:1060; :1062 is its `return` statement. Every sibling citation in the same sentence points at a signature line — inp_case_mode() :1036 (correct), inp_case_folding() :1041 (correct), ng_ideq() :1074 (correct) — so this one alone is inconsistent.

8. IMPRECISE (Root Cause, lines 247-251): "`inp_case_mode()` ... is the single authority every classified predicate already reads". `inp_case_folding()` (:1041-1043) and `inp_case_exact_ids()` (:1060-1063) read the static `ng_case_mode` directly and do not call `inp_case_mode()`; only Class C's `vec_name_eq()` (src/frontend/vectors.c:415) and `Evt_Node_Name_Eq()` (src/xspice/evt/evtplot.c:65) actually call it. True of the static variable, false as written of the function.

9. SILENTLY EDITED TRANSCRIPTS. Every command reproduced, but three quoted blocks are not literal output and only one elision is disclosed. (a) Lines 134-143, the `.spiceinit` block: both runs actually emit a third vector line, `vs#branch` / `Vs#branch`, which is dropped with no marker. (b) Lines 114-122, the second `-p` block: the near-miss warning prints TWICE, not once (the parenthetical does say "the doubled near-miss warning is doc/codex/issues/0046", so this one is disclosed, but the block is still presented as a transcript); the two experimental banners are disclosed as elided. (c) Lines 314-321, criterion 6: the `ngspice 10002 -> load hdr_option.raw` echo, the `Loading raw data file ("hdr_option.raw") ... done.` lines and the whole vector listing are dropped with no marker, and stderr (`Error: casemode: no such variable.`) is interleaved into stdout without saying so.

10. UNSHOWN MEASUREMENT (criterion 6, line 310): "Measured on both binaries" but only the build-ver_50 transcript is given. I ran both: /usr/local/bin/ngspice produces the identical result (`before` empty with `Error: casemode: no such variable.` on stderr, `after preserve`), so the claim holds — but the issue does not show it, unlike the two-binary block at lines 56-70 which does.

11. INTERNAL INCONSISTENCY (line 92-93): "The latch runs during start-up and never again" is contradicted by the issue's own criterion 2 (lines 269-273): "The enum can move under it at any subsequent `inp_readall()` — a `source` or an `.include` from inside a `.control` block is one, and `set_case_mode()` runs on *every* input file read". The first statement is only true of a session in which nothing is sourced.
